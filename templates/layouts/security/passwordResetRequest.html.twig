{% extends 'base.html.twig' %}

{% block body %}
  <div class="form">

    <h2 class="main__title">Password reset request</h2>

    {% if error %}
        <div class="alert alert-danger">{{ error }}</div>
    {% endif %}

    {{ form(passwordResetRequestForm) }}
  </div>
{% endblock %}
